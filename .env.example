# Copy to .env — never commit the real .env (it's in .gitignore)

# MongoDB Atlas connection string
MONGODB_URI=

# Any long random string, used to sign session cookies
SESSION_SECRET=

# Port to run on locally (Render sets this automatically in production)
PORT=3000
